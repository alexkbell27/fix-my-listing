import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Unlock unlimited reports — Fix My Listing",
};

export default function PricingLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
